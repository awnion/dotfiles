# WIFI passwords ubuntu KDE storage

All WIFI networks are stored here:

```bash
cd /etc/NetworkManager/system-connections/
ls -la
```

But all passwords are stored at KDE Wallet.
So we need to export/import kde wallet.
